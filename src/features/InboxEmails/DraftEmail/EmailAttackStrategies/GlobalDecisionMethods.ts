// import { useAppSelector } from "../../../../app/hooks";

// Adds a name for a given template, returns default if the
// user has not enabled real personal information on this 
// phishing email.
export function addName(def: string, fancy: string): string {
    if (true) {
        return fancy;
    } else {
        return def;
    }
}

// Use either a fancy/better link address, or the worse one depending on
// domain strength.
//
// TODO needs to have multiple values based on domain level rather than just fancy & default.
export function addLink(def: string, fancy: string): string {
    if (true) {
        return fancy;
    } else {
        return def;
    }
}